# API Reference

This folder is populated automatically by **TypeDoc** during the release pipeline. It reflects the exact public TypeScript API of the package at each release tag.

Local generation:

```bash
npm run docs:api
```

(npm script added in Phase 0.)

The generated content overrides anything in this folder; do not hand-edit files here.
